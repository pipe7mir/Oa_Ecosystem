# ===========================================
# OASIS Backend - Environment Variables
# ===========================================
# Copy this file to .env and configure values

# Server Port (default: 3000)
PORT=3000

# Database Configuration
# Option 1: PostgreSQL (RECOMMENDED for production)
# Get free PostgreSQL from: https://supabase.com or https://neon.tech
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Option 2: SQLite for local development (automatic if DATABASE_URL not set)
# No configuration needed - will use data/database.sqlite

# JWT Secret (Generate a random string for production)
JWT_SECRET=your-super-secret-key-change-in-production

# Email Configuration (Optional - for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# WhatsApp Configuration (Optional)
# WHATSAPP_API_URL=https://api.whatsapp.com
# WHATSAPP_TOKEN=your-token
